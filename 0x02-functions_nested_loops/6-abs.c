#include "main.h"

/**
 * _abs - Prints absolute value
 * @x: number to be checked
 * Return: Absolute value or 0
 */

int _abs(int x)
{
	if (x < 0)
	{
	int abs_val;

	abs_val = x = -1;
	return (abs_val);
	}
	return (x);
}
