#include "main.h"
/**
 * _isupper - Check if a letter is uppercase
 * @y: Number to be checked
 * Return: 1 if uppercase 0 else
 */

int _isupper(int y)
{
	if (y >= 65 && y <= 90)
	{
	return (1);
	}
	return (0);
}
