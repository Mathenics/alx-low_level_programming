#include "main.h"

/**
 * _isdigit - Check if character is a digit
 * @a: Number to be checked
 * Return: 1 if its a digit, 0 else
 */

int _isdigit(int a)
{
	if (a >= 48 && a <= 57)
	{
	return (1);
	}
	return (0);
}
